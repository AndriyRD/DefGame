import { HitPackage } from "./HitPackage"

export class HitBuffer {
    private readonly buffer = new Array<HitBuffer>
    private readonly hitPackage = new HitPackage(undefined, undefined) 
    
    private OnCanSend(){
        
    }

    constructor(){
        
    }
}